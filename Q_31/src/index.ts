const userNames : string[] = ['admin','Jennifer','Areej','Daniya','Ayan','Umal'];
    if(userNames.length === 0){
  console.log('We need to find some users!');
  }
else{
 let userNames = []
    console.log('We need to find some users!'   +  userNames.length)
  }

